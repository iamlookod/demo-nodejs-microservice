apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-db-express
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-db-express
  template:
    metadata:
      labels:
        app: mongo-db-express
    spec:
      containers:
        - name: mongo-db-express
          image: mongo-express
          ports:
            - containerPort: 8081
          env:
            - name: ME_CONFIG_MONGODB_ADMINUSERNAME
              valueFrom:
                secretKeyRef:
                  name: mongodb-secret
                  key: mongo-root-username
            - name: ME_CONFIG_MONGODB_ADMINPASSWORD
              valueFrom:
                secretKeyRef:
                  name: mongodb-secret
                  key: mongo-root-password
            - name: ME_CONFIG_MONGODB_SERVER
              valueFrom:
                configMapKeyRef:
                  name: mongo-db-configmap
                  key: mongo_url
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-db-express-service
spec:
  selector:
    app: mongo-db-express
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30000
